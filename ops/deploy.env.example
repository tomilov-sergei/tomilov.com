# Copy to .deploy/deploy.env and replace every value.
# .deploy/ is ignored by Git.

SERVER=tomilov-deploy@example.com
DEPLOY_KEY=.deploy/tomilov_site_deploy_key
REMOTE_ROOT=/srv/tomilov.com
REMOTE_STORAGE_ROOT=/srv/tomilov-data/tomilov.com
